[package]
name = "server-conf"
version = "0.9.0"
rust-version = "1.85"
authors.workspace = true
edition.workspace = true

[dependencies]
config = "0.15"
serde = { version = "1.0", features = ["derive"] }
